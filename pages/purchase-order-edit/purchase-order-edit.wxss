/* pages/purchase-order-edit/purchase-order-edit.wxss */

.container {
  height: 100vh;
  overflow-y: auto;
  box-sizing: border-box;
  padding-top: 120rpx;
  background: #f8f9fa;
}

.main-content {
  padding: 20rpx;
  margin: 0 auto;
  max-width: 750rpx;
  box-sizing: border-box;
}

/* 导航栏样式 */
.nav-bar {
  background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
  padding: 60rpx 30rpx 30rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
  color: #ffffff;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  overflow: hidden;
}

.nav-bar::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="20" cy="20" r="2" fill="rgba(255,255,255,0.1)"/><circle cx="80" cy="40" r="1.5" fill="rgba(255,255,255,0.1)"/><circle cx="40" cy="80" r="1" fill="rgba(255,255,255,0.1)"/></svg>');
  opacity: 0.3;
}

.nav-title {
  display: flex;
  align-items: center;
  z-index: 1;
}

.nav-icon {
  width: 48rpx;
  height: 48rpx;
  margin-right: 20rpx;
  border-radius: 8rpx;
}

.nav-text {
  font-size: 36rpx;
  font-weight: 600;
}

.nav-actions {
  z-index: 1;
}

.nav-btn {
  width: 60rpx;
  height: 60rpx;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  backdrop-filter: blur(10rpx);
  transition: all 0.3s ease;
}

.nav-btn:active {
  background: rgba(255, 255, 255, 0.3);
  transform: scale(0.9);
}

/* 表单区域样式 */
.form-section {
  margin-bottom: 30rpx;
}

.section-title {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20rpx;
  padding: 0 10rpx;
}

.section-icon {
  width: 32rpx;
  height: 32rpx;
  margin-right: 16rpx;
  border-radius: 4rpx;
}

.section-text {
  font-size: 32rpx;
  font-weight: 600;
  color: #333333;
  flex: 1;
}

.add-btn {
  display: flex;
  align-items: center;
  background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
  color: #ffffff;
  padding: 12rpx 24rpx;
  border-radius: 12rpx;
  font-size: 26rpx;
  font-weight: 500;
  transition: all 0.3s ease;
}

.add-btn:active {
  transform: scale(0.95);
}

.add-btn-text {
  font-weight: 500;
  margin-left: 8rpx;
}

/* 表单卡片样式 */
.form-card {
  background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
  border-radius: 16rpx;
  padding: 30rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
}

.form-item {
  display: flex;
  align-items: center;
  padding: 20rpx 0;
  border-bottom: 1rpx solid #f0f0f0;
}

.form-item:last-child {
  border-bottom: none;
}

.form-label {
  font-size: 28rpx;
  color: #333333;
  font-weight: 500;
  width: 160rpx;
  flex-shrink: 0;
}

.form-input {
  flex: 1;
  height: 60rpx;
  font-size: 28rpx;
  color: #333333;
  background: #f8f9fa;
  border-radius: 8rpx;
  padding: 0 20rpx;
  border: 1rpx solid #e9ecef;
  transition: all 0.3s ease;
}

.form-input:focus {
  border-color: #43e97b;
  background: #ffffff;
}

.form-input:disabled {
  color: #999999;
  background: #f0f0f0;
}

.form-textarea {
  width: 100%;
  min-height: 120rpx;
  font-size: 28rpx;
  color: #333333;
  background: #f8f9fa;
  border-radius: 8rpx;
  padding: 20rpx;
  border: 1rpx solid #e9ecef;
  transition: all 0.3s ease;
  box-sizing: border-box;
}

.form-textarea:focus {
  border-color: #43e97b;
  background: #ffffff;
}

.form-picker {
  flex: 1;
}

.picker-content {
  display: flex;
  align-items: center;
  background: #f8f9fa;
  border-radius: 8rpx;
  padding: 16rpx 20rpx;
  border: 1rpx solid #e9ecef;
  transition: all 0.3s ease;
}

.picker-content:active {
  background: #e9ecef;
}

.status-indicator {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  margin-right: 12rpx;
}

.status-indicator.status-pending {
  background: #ffc107;
}

.status-indicator.status-confirmed {
  background: #17a2b8;
}

.status-indicator.status-purchasing {
  background: #007bff;
}

.status-indicator.status-completed {
  background: #28a745;
}

.status-indicator.status-cancelled {
  background: #dc3545;
}

.picker-text {
  font-size: 28rpx;
  color: #333333;
  flex: 1;
}

/* 商品列表样式 */
.products-list {
  background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
  border-radius: 16rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
}

.product-item {
  display: flex;
  align-items: center;
  padding: 30rpx;
  border-bottom: 1rpx solid #f0f0f0;
}

.product-item:last-child {
  border-bottom: none;
}

.product-info {
  display: flex;
  align-items: center;
  flex: 1;
}

.product-icon {
  width: 60rpx;
  height: 60rpx;
  margin-right: 20rpx;
  border-radius: 8rpx;
}

.product-details {
  display: flex;
  flex-direction: column;
}

.product-name {
  font-size: 28rpx;
  color: #333333;
  font-weight: 500;
  margin-bottom: 8rpx;
}

.product-sku {
  font-size: 24rpx;
  color: #666666;
}

.product-actions {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.quantity-control {
  display: flex;
  align-items: center;
  background: #f8f9fa;
  border-radius: 8rpx;
  overflow: hidden;
}

.quantity-btn {
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #e0e0e0;
  transition: all 0.3s ease;
}

.quantity-btn:active {
  background: #d0d0d0;
}

.quantity-text {
  width: 80rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  color: #333333;
  font-weight: 500;
  background: #ffffff;
}

.product-price {
  font-size: 28rpx;
  color: #43e97b;
  font-weight: 600;
  min-width: 80rpx;
  text-align: right;
}

.product-unit {
  font-size: 24rpx;
  color: #999999;
  background: #f0f0f0;
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
}

.remove-btn {
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(220, 53, 69, 0.1);
  border-radius: 50%;
  transition: all 0.3s ease;
}

.remove-btn:active {
  background: rgba(220, 53, 69, 0.2);
  transform: scale(0.9);
}

/* 空状态样式 */
.empty-products {
  text-align: center;
  padding: 60rpx 30rpx;
  background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
  border-radius: 16rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
}

.empty-icon {
  width: 120rpx;
  height: 120rpx;
  margin-bottom: 20rpx;
  opacity: 0.5;
}

.empty-text {
  font-size: 32rpx;
  color: #666666;
  font-weight: 500;
  display: block;
  margin-bottom: 10rpx;
}

.empty-hint {
  font-size: 26rpx;
  color: #999999;
  display: block;
}

/* 汇总区域样式 */
.summary-section {
  margin-bottom: 30rpx;
}

.summary-card {
  background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
  border-radius: 16rpx;
  padding: 30rpx;
  display: flex;
  justify-content: space-between;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
}

.summary-item {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.summary-label {
  font-size: 24rpx;
  color: #666666;
  margin-bottom: 8rpx;
}

.summary-value {
  font-size: 32rpx;
  color: #333333;
  font-weight: 600;
}

.summary-value.total {
  font-size: 36rpx;
  color: #43e97b;
  font-weight: 700;
}

/* 操作按钮样式 */
.action-buttons {
  display: flex;
  gap: 20rpx;
  margin: 40rpx 0;
  padding: 0 10rpx;
}

.cancel-btn {
  flex: 1;
  background: #f8f9fa;
  border-radius: 16rpx;
  padding: 30rpx;
  text-align: center;
  border: 1rpx solid #e9ecef;
  transition: all 0.3s ease;
}

.cancel-btn:active {
  background: #e9ecef;
  transform: scale(0.98);
}

.cancel-btn-text {
  font-size: 32rpx;
  color: #666666;
  font-weight: 500;
}

.save-btn {
  flex: 1;
  background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
  border-radius: 16rpx;
  padding: 30rpx;
  text-align: center;
  box-shadow: 0 4rpx 20rpx rgba(67, 233, 123, 0.3);
  transition: all 0.3s ease;
}

.save-btn:active {
  transform: scale(0.98);
  box-shadow: 0 2rpx 10rpx rgba(67, 233, 123, 0.4);
}

.save-btn-text {
  font-size: 32rpx;
  color: #ffffff;
  font-weight: 600;
}